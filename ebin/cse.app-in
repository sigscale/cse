{application, %APP_NAME%,
	[{description, "CAMEL Service Environment (CSE)"},
		{vsn, "%APP_VERSION%"},
		{modules,
				[cse,
				cse_app,
				cse_sup,
				cse_server,
				cse_sap_sup_sup,
				cse_sap_sup,
				cse_slp_sup,
				cse_slp_fsm]},
		{registered, [cse]},
		{applications, [m3ua, sccp, tcap, map, cap]},
		{env, []}]}.
